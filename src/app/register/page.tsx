import React from "react";
import { RegisterForm } from "./Form";

const page = () => {
  return (
    <div className="section_padding py-20 flex justify-center">
      <RegisterForm />
    </div>
  );
};

export default page;
